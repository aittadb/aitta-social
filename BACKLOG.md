# AittaSocial backlog

This file contains unscheduled possibilities. Backlog items are not commitments
and must not introduce placeholder tables, routes, interfaces, or empty
abstractions. Stable `BACKLOG-NNN` identifiers are never renumbered or reused;
accepted work must be promoted to `PLAN.md` before implementation.

There are currently no unscheduled backlog items. On 2026-08-09 the owner
accepted and promoted the previous network candidates:

- `BACKLOG-001` (following presences) is represented by `TASK-047` and
  `TASK-048`.
- `BACKLOG-002` (combined followed-presence reader) is represented by
  `TASK-049` and `TASK-050`.
- `BACKLOG-003` (presence discovery) is represented by `TASK-045` and
  `TASK-046`.

The mapped task IDs preserve promotion provenance. `PLAN.md` is authoritative
for unfinished work and `CHANGELOG.md` for completed work; this file makes no
current-capability claim.
