# AittaSocial implementation plan

This is the accepted, unfinished queue. IDs are stable. Finished work moves to
`CHANGELOG.md`; unscheduled possibilities belong in `BACKLOG.md`. Every item is
a bounded vertical slice whose definition of done includes its relevant
implementation, negative tests, security and privacy review, accessibility,
migration and protocol decisions, documentation, and decisive evidence.

## Audit basis

- On 2026-08-11 the repository POC and public Site were rechecked. Public
  profile and published-update HTML, protocol 1.0 discovery and `/api/v1`, the
  sole-owner boundary, draft privacy, deployment-owned D1, native navigation,
  responsive behavior, and Hub-failure isolation are working and remain in
  scope without being rewritten.
- The one existing public Site now runs version 8 from exact owner-reviewed
  `main` source commit `18fa16dc967d8502c17afb2bd3cc28518039a172`.
  The saved version and terminal deployment report that exact commit, and the
  preserved access, protected-environment metadata, D1 binding, and custom
  domain evidence is recorded in `docs/checkpoint.md` and
  `docs/acceptance/task-157-mobile-presence-deployment.md`.
- No accepted Aitta-consumable Hub contract currently exists. Secure Hub
  registration, verified discovery, Follow and Unfollow, and a private
  followed-update reader therefore remain future direction in `ROADMAP.md`.
  They return to this queue only one bounded vertical increment at a time after
  their exact external contract or service prerequisite exists; endpoints,
  claims, tokens, and data ownership must never be guessed.
- The public deployment now uses the accepted compact, identity-led mobile
  profile experience documented in
  `docs/acceptance/task-156-mobile-presence-redesign.md`. The authenticated
  owner workspace deliberately retained its earlier sidebar, editorial panels,
  long form hierarchy, and wrapped phone navigation, so it remains the next
  accepted visual and interaction gap rather than a completed redesign claim.
- Exact deployed source validation passes the instruction, license, plan,
  instance, runtime, D1 migration, type, lint, production-build, and 191 test
  gates; migration generation reports no change and the production dependency
  audit reports no advisory. The active owner-workspace tasks must preserve
  those product, authorization, privacy, and public-contract boundaries.
- The next increment establishes only a bounded AittaSocial-specific visual
  vocabulary and simpler owner journeys. It is not authority for a generic UI
  framework, general theme system, new persisted customization, public/owner
  authorization coupling, or new capability. The 34 former Hub and network
  tasks remain future provenance under `ROADMAP-004` through `ROADMAP-008`,
  while more speculative lifecycle and suggestion work remains in
  `BACKLOG.md`.

## Active queue

This remains one flat dependency graph rather than phased work. Once every
recorded dependency is complete, tasks without an edge between them are
intended to be parallelizable when their declared files and migration surfaces
are disjoint. Each task owns one bounded outcome plus its tests, documentation,
and migration/configuration decision; split it again before implementation if
those cannot land as one focused reviewable commit.

- [ ] **TASK-162 — Clarify restrained Identity appearance controls and preview.** Make the existing accent, density, attribution, and transient Identity preview a compact secondary owner choice with unmistakable saved-versus-unsaved meaning. DoD: preserve the constrained accent resolver, raw stored preference, density and attribution payloads, reload behavior, public rendering, API, authorization, D1, and protocol; add no runtime theme, arbitrary style input, new profile field, schema, or configuration; prove saved and unsaved preview, valid and invalid historical accents, both densities, visible and hidden attribution, failure/reload, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-161.
- [ ] **TASK-163 — Make each update kind self-explanatory while composing.** Add kind-specific, plain-language requirements and help to the compact composer without losing values when the owner changes kind. DoD: keep note body primary; make article, announcement, and link requirements clear; preserve all entered fields through kind changes and retain current validation, payload, create/edit API, authorization, D1, draft privacy, and public contracts; prove every kind, kind switching, destination-required validation, long content, failure/recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-160.
- [ ] **TASK-165 — Make deleting an update a separate explicit irreversible owner action.** Make per-update deletion understandable, confirmable, and recoverable without competing with normal editing or publication controls, using accepted `Aitta` terminology in the deletion and recovery copy this task owns. DoD: preserve the current delete endpoint, update-specific accessible label, confirmation boundary, authorization, D1 deletion, unknown-result recovery, redirect behavior, draft privacy, public unknown parity, API, and protocol behavior; add no automatic retry, background deletion, schema, or network feature; prove draft and published deletion confirmation/cancellation, definitive failure, unconfirmed recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-164.
- [ ] **TASK-177 — Use one coherent public Aitta frame on every human public page.** Centralize the product-specific public header, navigation, footer, and page-frame composition so configured and unconfigured home, published permalink, Privacy, Technical, storage-unavailable, and global/draft/unknown not-found states reuse the same maintained chrome. DoD: keep one stable public-header navigation across public routes instead of replacing `Manage` with page-specific actions; keep Privacy, Technical, and GitHub available in the common footer and only the powered-by attribution owner-hideable; preserve every route, metadata, API, D1, owner destination, draft-unknown parity, CSP and cache behavior; leave the distinct authorized owner workspace and safe access states to TASK-182; add no generic UI framework, theme system, client router, sticky obstruction, or fake destination; prove every named public page/state at 320/390/1440 pixels and 400-percent reflow with native keyboard/focus, 44-pixel touch targets, forced colors, reduced motion, no overflow/private canary/console error, focused shared-chrome regression tests and an acceptance record, migration-drift/full-validation/audit/diff gates, and no external mutation. Depends on: TASK-176.
- [ ] **TASK-178 — Establish the prerelease v1 integration root and JSON boundary.** Establish the single versioned integration API at D1-independent `GET /api/v1` and `GET /api/v1/schema`; `/api/v2` must not exist. DoD: introduce only small product-specific v1 representation, ordered link/action, structured-error, and bounded JSON-media helpers. With a normalized protected canonical URL, root is exactly `data:{id:"aitta-social-api",type:"api",attributes:{name:"AittaSocial",version:1}}`, ordered canonical `self`, API-schema `profile`, and discovery-manifest `social.aitta.manifest` links, and empty actions. Schema is exactly `data:{id:"aitta-social-api-profile",type:"api-profile",attributes:{version:1,representation:"aitta-social-json-api-v1",relations:["self","profile","collection","social.aitta.manifest"]}}`, ordered canonical `self` and root `collection` links, and empty actions. Add exactly `endpoints.api:"{canonical}/api/v1"` to the configured manifest; keep its `profile` and `entries` members/values unchanged. Root/schema and unknown v1 paths are JSON-only: missing, wildcard, or JSON-compatible `Accept` yields JSON; explicit JSON exclusion, malformed syntax, or input above 4 KiB/16 ranges yields no-store JSON `406`; no API boundary selects HTML, redirect, User-Agent, or query format. They produce bounded JSON `405` plus exact `Allow`, `404`, and safe `500` responses with `Vary: Accept`; `HEAD` has matching headers and an empty body. TASK-179–181 must apply that same policy to the known site/entry routes when they reshape them. Missing or invalid canonical configuration gives structured no-store `503` without D1, request-host, owner, Hub, or other protected-runtime fallback. This deliberately establishes the pre-release v1 baseline: it leaves the existing site/entry resource shapes unchanged only until their own accepted rows, rather than promising a v2 compatibility lane. Prove exact root/schema/manifest link and unknown shapes, canonical/no-D1/hostile-host/private-canary boundaries, JSON/default/wildcard/q-zero/tie/malformed/oversized media, every conventional root/schema/unknown method including HEAD/OPTIONS, headers/cache/content types, and existing site/entry behavior pending their replacement; update protocol/API documentation and acceptance evidence; pass migration-drift/full-validation/audit/diff gates without schema or external mutation. Exclusive ownership: `lib/api-v1/**`, v1 root/schema/unknown boundary, manifest discovery link, focused v1-root tests, and direct documentation. Depends on: none.
- [ ] **TASK-179 — Reshape the prerelease v1 public Aitta profile resource.** Replace `GET /api/v1/site`'s unshipped response grammar with the TASK-178 v1 singleton profile resource and advertise it from the v1 root. DoD: return only the existing explicit public profile allowlist—including the required protocol compatibility `accountType` value—in `data:{id:"profile",type:"profile",attributes:{...}}`, ordered canonical `self`, API-schema `profile`, and namespaced `social.aitta.profile` links, and empty anonymous actions; retain public-without-Hub access, canonical authority, optional-field omissions, setup/storage status categories, and owner/private-canary exclusion while deliberately updating pre-release v1 envelope, error, media, cache, manifest, tests, and protocol documentation together. Inherit JSON-only v1 `Accept`, errors, `405`, `HEAD`, unknown-route, canonical, and hostile-host behavior; add no browser-owner or machine action, schema, Hub, or external mutation. Prove configured, absent-profile, invalid-canonical, storage failure, identity/runtime/draft canaries, exact root/manifest discovery, relation selection, and no regression in public facts or privacy; pass migration-drift/full-validation/audit/diff gates. Exclusive ownership: v1 site route/representation, additive v1-root/manifest capability link, focused site tests, and direct documentation. Depends on: TASK-178.
- [ ] **TASK-180 — Reshape the prerelease v1 published-updates collection.** Replace `GET /api/v1/entries`'s unshipped response grammar with the TASK-178 v1 published collection and advertise it from the v1 root. DoD: return only published entry resources in the accepted collection envelope, deterministic existing order, bounded `page`/`pageSize`, canonical `self`, `first`, `last`, `previous`, and `next` links where applicable, and empty anonymous actions; derive `last` from one explicit published-only count using the same predicate as the page query, define an empty collection's last page as 1, and never count, scan, identify, or otherwise leak a draft. Establish the collection's cache partition now: anonymous JSON is `public, max-age=30`, empty-action, and `Vary: Accept, Authorization`, so TASK-191 can later add a no-store service-actor action without a cache leak. Deliberately update the pre-release v1 envelope, pagination/error/cache documentation, manifest/root discovery, and tests together while preserving public-without-Hub access, canonical authority, all four update kinds, published-only data semantics, and draft/private canary boundaries. Inherit TASK-178's JSON-only media/errors/methods and add no schema, browser-owner, Hub, or external mutation. Prove empty/one/many/first/last/next/previous/beyond-end/invalid pages, deterministic repeats, draft-only/mixed boundary counts, absent profile, invalid canonical, storage failure, hostile host, exact cache/Vary, and private canaries; pass migration-drift/full-validation/audit/diff gates. Exclusive ownership: v1 collection route and entry-collection/count representations, additive root/manifest collection link, focused collection tests, and direct documentation. Depends on: TASK-179.
- [ ] **TASK-181 — Reshape the prerelease v1 published-update detail resource.** Replace `GET /api/v1/entries/{id}`'s unshipped response grammar with the TASK-178 v1 entry resource and advertise it from the v1 root. DoD: v1 stays JSON-only and returns one published entry with canonical API `self`, collection, and human `alternate` links plus empty anonymous actions; deliberately update the pre-release v1 envelope, error/media/cache documentation, manifest/root discovery, and tests together. Preserve identical timing-independent status/error privacy for draft, unpublished, deleted, malformed, and unknown identifiers; canonical authority, every update kind/optional omission, no Hub dependency, and private-canary exclusion remain exact. Inherit TASK-178 media/errors/method behavior; add no unversioned negotiation, browser-owner action, schema, Hub, or external mutation. Prove all four kinds, draft/unknown/deleted parity, canonical/setup/storage/hostile-host failures, identifier encoding, links/cache, and private canaries; update protocol/API documentation and acceptance evidence; pass migration-drift/full-validation/audit/diff gates. Exclusive ownership: v1 entry-detail representation/route, additive root/manifest detail link, focused detail tests, and direct documentation. Depends on: TASK-180.
- [ ] **TASK-182 — Reuse the common Aitta chrome in the private owner workspace.** Adapt the authorized `OwnerShell` and safe owner-access states to the same maintained presentational header, footer, link, focus, touch, and responsive primitives established by the public frame while retaining an unmistakably private administration context and owner-specific navigation. DoD: keep exactly one main landmark and one logical skip target; retain the distinct three-link Home, Identity, and New update navigation with active-page semantics, plus View Aitta and the exact existing sign-out destination; make Privacy, Technical, and GitHub at least 44 pixels and reachable through the common footer without making shared primitives read auth, D1, profile, or runtime state; preserve authorization-before-D1 behavior and expose no owner identity, draft, protected configuration, or authorization-result canary in safe access states; preserve every owner route, mutation, API, D1, public projection, CSP, metadata, and cache boundary; add no generic UI framework, client router, public/owner authorization coupling, fake destination, duplicate navigation landmark, or sticky obstruction; prove configured owner home/profile/new/edit, signed-out redirect, non-owner, and missing-owner states at 320/390/1440 pixels and 400-percent reflow with native keyboard/focus, 44-pixel touch targets, forced colors, reduced motion, no overflow/private-canary/console error, focused shared-chrome and owner-security regressions and an acceptance record, migration-drift/full-validation/audit/diff gates, and no external mutation. Depends on: TASK-170, TASK-177.
- [ ] **TASK-183 — Restore reviewed engineering-policy publication.** Use an ordinary authenticated, non-rewriting push to make reviewed local multi-agent-policy commit `ec96d4d` and the completed TASK-184 guidance commit reachable from `origin/develop`. DoD: verify the remote branch contains both commits and their local/remote policy bytes match; if authentication remains unavailable after TASK-184, retain this task as the explicit external blocker rather than treating the recorded failure as completion; and make no `main`, application, schema, migration, Site, deployment, data, setting, access, DNS, domain, Hub, sibling, or runtime change. Depends on: TASK-184.
- [ ] **TASK-188 — Safely import and preview one custom-page fragment.** Implement the pure bounded HTML-fragment-to-`PageDocumentV1` compiler and one authorized owner import/normalized-preview journey. It stores and publishes nothing. DoD: an authorized sole owner can enter a bounded title, optional description, and HTML fragment; receive and inspect the exact normalized `PageDocumentV1`; and see it through an exhaustive escaped preview. Exact default/annotated layout, fragment, link-group, same-page fragment, updates, and external-link mappings work while relative page links remain unavailable without a trusted resolver. Unsupported full documents, shell/header/nav/footer source, active/unknown elements or attributes, hostile/malformed URLs, malformed trees, duplicate fragments, and every byte/node/depth/text/link bound fail as one safe non-reflective error; raw input appears only escaped in its owner textarea and never as interpreted HTML/style, hydration markup, logs, or a persisted value; the preview endpoint enforces owner authorization, exact same origin, JSON media type, a feature-local body bound, strict output allowlist, and no outbound fetch; prove non-owner/missing-owner, injection/private canaries, denial, parser differentials, long/unbroken and representative flow/split/cards content, native safe links, 320/390/1440 widths, 400-percent reflow, keyboard/focus/touch, forced colors, reduced motion, no overflow, and clean console; pass production audit and full repository/diff gates without persistence or external mutation. Exclusive ownership: new `lib/custom-pages/**`, new `app/api/private/pages/preview/route.ts`, `app/owner/pages/import/**`, one owner-specific navigation entry in `OwnerShell` after TASK-182, a feature-owned preview style module, a pinned Worker-compatible HTML-parser dependency only if review proves it required, and focused importer/preview tests and acceptance documentation. Do not change D1/schema/migrations, public routes, profile/entry contracts, root-home selection, public-frame composition, design/CSS, assets, or hosting. Depends on: TASK-182, TASK-187.
- [ ] **TASK-190 — Verify the Sites identity-header ingress boundary without mutation.** Resolve the exact external blocker recorded by TASK-189 before any new browser-owner authorization adapter relies on forwarded identity. DoD: with explicit owner approval, re-enumerate every current Sites-provided and custom production origin from authoritative hosting state, then exercise one existing read-only owner page on each origin without retaining its body: anonymous with no identity headers, forged configured-owner email plus arbitrary user ID, forged non-owner email plus arbitrary user ID, normal signed-in owner, and normal signed-in non-owner; use no form submission, API mutation, content write, deployment, setting/access/domain change, or private-body capture; record status/redirect/access classification and whether caller-supplied identity headers are stripped, overwritten, or forwarded, separately for each origin and for local/non-Sites mode; reconcile that evidence with the current official Sites contract and classify it as verified ingress reliance, a documented signed assertion with exact verification fields, a demonstrated vulnerability, or unresolved; never print either email, a header value, cookie, token, response body, hosting identifier, or secret, and never use Host, Origin, Referer, User-Agent, IP, or the header name as proof; add one acceptance record and update only the trust documentation required by the result. This task remains an explicit external blocker until the owner supplies the approval, authoritative origin inventory, configured-owner test input through a non-recorded channel, and separate owner/non-owner browser sessions. Depends on: TASK-189.
- [ ] **TASK-191 — Let one deployment-bound machine actor create a private v1 draft.** Add the first and only machine mutation: scoped `POST /api/v1/entries` creates one server-forced private draft and extends TASK-180's collection representation so only a currently authenticated `entries:write` service actor discovers its `rel: create` action. DoD: implement a machine-authentication adapter separate from Sites browser identity that accepts only bounded protected current/next `Authorization: Bearer` credential slots with non-secret credential ID, deployment audience, scope, expiry, and secret; use constant-time Web Crypto verification, canonical Aitta audience binding, overlap rotation, immediate removal/expiry revocation, and fail closed for missing/malformed/duplicate configuration, missing/wrong scheme, unknown ID, invalid secret, expiry, audience, or scope. Never accept `oai-authenticated-user-*`, cookies, email, query/body identity, ChatGPT, Codex, a prompt, or the human owner as the machine actor; grant no `/owner`, browser-private API, publish, edit, delete, configuration, Hub, or general administration. Accept only TASK-178 JSON/media input and one allowlisted draft-create payload; reuse the existing validated prepared entry-create service, force server-owned draft state/identifiers/timestamps, and return no-store `201` in TASK-180's entry representation with only safe draft facts and a canonical collection link. Keep ordinary no-credential collection reads public cached with empty actions; make a valid service-credential collection no-store with `rel:create`; return no-store 401 for invalid presented credentials and 403 only for authenticated wrong scope; keep `Vary: Accept, Authorization` on every collection variant and no-store on POST/error responses. Add one reviewed migration and atomic bounded audit record for every authenticated attempt's credential ID, fixed machine actor, operation, target identifier when allocated, safe outcome, correlation ID, and timestamp—never secret/header/body/content, owner email, Sites identity, stack, SQL, or response content; unauthenticated invalid attempts create no attacker-controlled audit rows. Prove action visibility/cache order, draft isolation from all public HTML/v1 projections/counts, every media/auth/validation failure, forged Sites headers/cookies/owner email, rotation/revocation/scope/audience/replay/audit redaction and atomicity, browser-owner non-regression, and exact cache behavior; update API/security/deployment/reproducibility documentation and acceptance evidence and pass migration/full-validation/audit/diff gates. No hosted secret may be created, changed, read, or tested without explicit owner approval of exact target-Site slots; without it, hosted completion remains blocked. Add no other machine operation, token minting, Hub credential, OAuth/OIDC, dashboard access, schema generalization, or external mutation. Exclusive ownership: machine-auth/config types, `POST` and authenticated-action extension on `app/api/v1/entries/route.ts`, one audit migration/repository operation, focused machine-create tests, and direct documentation. Depends on: TASK-180.
- [ ] **TASK-192 — Make the private Identity mutation a truthful JSON-first browser API.** Normalize only `PUT /api/private/profile` and its authorized Identity client around an explicit JSON success/error representation, without making it a machine credential or public resource. DoD: accept only bounded JSON with the existing same-origin-before-owner authorization ordering and existing profile validation; return an allowlisted owner-profile resource, canonical navigation links, and only actions currently available to the verified browser owner instead of an empty `204`; return structured JSON for malformed body, unsupported media (`415`), domain validation (`422`), authorization, method (`405` plus `Allow`), storage, and unexpected safe failures; preserve exact protected-setting secrecy, no-D1 denial behavior, browser save/reload/dirty/recovery semantics, D1 fields, public projection, and existing owner route. Reuse the TASK-178 JSON/error types only where their public/private privacy rules fit; do not expose this route through discovery, grant machine access, change another private route, add schema, or weaken browser CSRF. Prove authorized save/reload, malformed/media/validation/4xx/5xx recovery, non-owner/missing-owner/same-origin denial before D1, private canaries, browser-client compatibility, JSON headers/body/cache, and all existing public contracts; update focused tests, security/presentation documentation, and acceptance evidence; pass migration-drift/full-validation/audit/diff gates without external mutation. Exclusive ownership: private profile route/client response handling, feature-local representation bridge, focused Identity/API tests, and direct documentation. Depends on: TASK-178.
- [ ] **TASK-193 — Negotiate current hypermedia JSON for a published update document.** Make the existing unversioned `/entries/{id}` document return either its normal HTML or its current hypermedia JSON representation at the same canonical URI, without changing the completed TASK-181 v1 integration route. DoD: only bounded `Accept` chooses a variant—missing/wildcard or HTML/JSON tie returns unchanged HTML, explicitly JSON-preferred input returns JSON whose `self` is the same unversioned URI and whose `alternate` is HTML, and malformed/excluded/unsupported input returns safe `406` with `Vary: Accept`; never use User-Agent, query selection, or `/vN` path. Reuse TASK-181's bounded public entry projection/service; do not duplicate D1 logic or create a generic pathname framework. Because a Vinext page and route cannot coexist at one segment, own and prove one narrow Worker/Vinext-supported internal dispatch for this route only that excludes `/api/*`, `/.well-known/*`, `/owner/*`, Sites navigation, framework, and static paths, strips/rejects externally supplied internal markers, cannot bypass page authorization or public projection, and preserves CSP/no-store HTML behavior. Preserve identical timing-independent status/error/body privacy for draft, unpublished, deleted, malformed, and unknown identifiers across HTML and JSON; preserve canonical authority, every update kind/optional omission, no Hub dependency, and private-canary exclusion. Prove compiled-Worker HTML/JSON variants, q/value/order/tie behavior, draft/unknown/deleted parity, canonical/setup/storage/hostile-host failures, links/cache/CSP, identifier encoding, native HTML navigation, and focused rendered accessibility evidence; update hypermedia/presentation documentation and acceptance evidence; pass migration-drift/full-validation/audit/diff gates without schema or external mutation. Future published human documents such as `/about` must reuse this proven unversioned pattern through their own task, not a `/vN` page path. Exclusive ownership: one explicit entry-document negotiation adapter, focused compiled-Worker tests, and direct documentation. Depends on: TASK-181.
