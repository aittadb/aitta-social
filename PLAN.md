# AittaSocial implementation plan

This is the accepted, unfinished queue. IDs are stable. Finished work moves to
`CHANGELOG.md`; unscheduled possibilities belong in `BACKLOG.md`. Every item is
a bounded vertical slice whose definition of done includes its relevant
implementation, negative tests, security and privacy review, accessibility,
migration and protocol decisions, documentation, and decisive evidence.

## Audit basis

- On 2026-08-11 the repository POC and public Site were rechecked. Public
  profile and published-update HTML, protocol 1.0 discovery and `/api/v1`, the
  sole-owner boundary, draft privacy, deployment-owned D1, native navigation,
  responsive behavior, and Hub-failure isolation are working and remain in
  scope without being rewritten.
- The one existing public Site now runs version 8 from exact owner-reviewed
  `main` source commit `18fa16dc967d8502c17afb2bd3cc28518039a172`.
  The saved version and terminal deployment report that exact commit, and the
  preserved access, protected-environment metadata, D1 binding, and custom
  domain evidence is recorded in `docs/checkpoint.md` and
  `docs/acceptance/task-157-mobile-presence-deployment.md`.
- No accepted Aitta-consumable Hub contract currently exists. Secure Hub
  registration, verified discovery, Follow and Unfollow, and a private
  followed-update reader therefore remain future direction in `ROADMAP.md`.
  They return to this queue only one bounded vertical increment at a time after
  their exact external contract or service prerequisite exists; endpoints,
  claims, tokens, and data ownership must never be guessed.
- The public deployment now uses the accepted compact, identity-led mobile
  profile experience documented in
  `docs/acceptance/task-156-mobile-presence-redesign.md`. The authenticated
  owner workspace deliberately retained its earlier sidebar, editorial panels,
  long form hierarchy, and wrapped phone navigation, so it remains the next
  accepted visual and interaction gap rather than a completed redesign claim.
- Exact deployed source validation passes the instruction, license, plan,
  instance, runtime, D1 migration, type, lint, production-build, and 191 test
  gates; migration generation reports no change and the production dependency
  audit reports no advisory. The active owner-workspace tasks must preserve
  those product, authorization, privacy, and public-contract boundaries.
- The next increment establishes only a bounded AittaSocial-specific visual
  vocabulary and simpler owner journeys. It is not authority for a generic UI
  framework, general theme system, new persisted customization, public/owner
  authorization coupling, or new capability. The 34 former Hub and network
  tasks remain future provenance under `ROADMAP-004` through `ROADMAP-008`,
  while more speculative lifecycle and suggestion work remains in
  `BACKLOG.md`.

## Active queue

This remains one flat dependency graph rather than phased work. Once every
recorded dependency is complete, tasks without an edge between them are
intended to be parallelizable when their declared files and migration surfaces
are disjoint. Each task owns one bounded outcome plus its tests, documentation,
and migration/configuration decision; split it again before implementation if
those cannot land as one focused reviewable commit.

- [ ] **TASK-160 — Simplify basic private-draft composition and editing.** Make creating or editing an update a compact mobile-first form with body-first authoring, clear draft/private wording, one unambiguous save action, explicit definitive-error or unconfirmed-result recovery, and accepted `Aitta` terminology in the composer and return-path copy this task owns. DoD: preserve all four stored kinds and every entered title, body, and destination value, current create/edit routes and API payloads, draft privacy, authorization, idempotency, D1 behavior, and public contracts; add no auto-publish, background retry, schema, generic editor framework, or network feature; prove new, draft, published, long/unbroken, validation, failure, recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-159.
- [ ] **TASK-161 — Make optional public Identity details compact and discoverable.** Recompose only location, website, and external-link editing as a clearly secondary, mobile-safe Identity section that does not compete with saving required Identity. DoD: preserve exact optional-field payloads, URL validation, eight-link limit, native labels, saved values, API, authorization, D1, protocol, and public rendering; prove absent, one, eight, long/unbroken, validation, save/reload, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-159.
- [ ] **TASK-162 — Clarify restrained Identity appearance controls and preview.** Make the existing accent, density, attribution, and transient Identity preview a compact secondary owner choice with unmistakable saved-versus-unsaved meaning. DoD: preserve the constrained accent resolver, raw stored preference, density and attribution payloads, reload behavior, public rendering, API, authorization, D1, and protocol; add no runtime theme, arbitrary style input, new profile field, schema, or configuration; prove saved and unsaved preview, valid and invalid historical accents, both densities, visible and hidden attribution, failure/reload, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-161.
- [ ] **TASK-163 — Make each update kind self-explanatory while composing.** Add kind-specific, plain-language requirements and help to the compact composer without losing values when the owner changes kind. DoD: keep note body primary; make article, announcement, and link requirements clear; preserve all entered fields through kind changes and retain current validation, payload, create/edit API, authorization, D1, draft privacy, and public contracts; prove every kind, kind switching, destination-required validation, long content, failure/recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-160.
- [ ] **TASK-164 — Make publish and unpublish a clear per-update lifecycle choice.** Recompose dashboard and edit-page publish/unpublish controls around direct Draft and Published meaning, explicit publication confirmation, honest state-result recovery, and accepted `Aitta` terminology in the lifecycle copy this task owns. DoD: preserve current state endpoint, update-specific accessible labels, confirmation boundary, idempotency, authorization, D1 state transitions, draft/unknown parity, public projection, cache, metadata, API, and protocol behavior; add no automatic publish, retry, fake social control, schema, or network feature; prove draft and published rows, publish confirmation and cancellation, unpublish, definitive failure, unconfirmed recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-160.
- [ ] **TASK-165 — Make deleting an update a separate explicit irreversible owner action.** Make per-update deletion understandable, confirmable, and recoverable without competing with normal editing or publication controls, using accepted `Aitta` terminology in the deletion and recovery copy this task owns. DoD: preserve the current delete endpoint, update-specific accessible label, confirmation boundary, authorization, D1 deletion, unknown-result recovery, redirect behavior, draft privacy, public unknown parity, API, and protocol behavior; add no automatic retry, background deletion, schema, or network feature; prove draft and published deletion confirmation/cancellation, definitive failure, unconfirmed recovery, denial, and private-canary states at 320, 390, and 1440 pixels with keyboard, focus, touch, 400-percent reflow, reduced motion, forced colors, and clean-console evidence; add focused regression coverage, presentation/journey documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-164.
- [ ] **TASK-170 — Finish Aitta terminology in the owner shell and home.** Replace only the residual application-level `presence` wording in `OwnerShell`, owner access states, and the owner home/readiness summary with concise `Aitta` language. DoD: keep `profile` or Identity for outward presentation and keep update-state wording unchanged; preserve sole-owner authorization-before-D1 behavior, protected-setting secrecy, authenticated-identity privacy, navigation destinations, readiness derivation, counts, first-update journey, and every action; do not touch profile-form, composer, publish/unpublish, or delete behavior owned by TASK-159–165, or change APIs, schema, or configuration; retain internal identifiers; prove fresh/incomplete/complete, zero/draft/published/many updates, unconfigured owner, non-owner, private-canary, 320-pixel and 400-percent reflow, keyboard/focus, touch, and clean-console paths; update direct owner-shell/home tests, presentation documentation, and an acceptance record; and pass migration-drift, full validation, production audit, and diff gates without external mutation. Depends on: TASK-164, TASK-169.
- [ ] **TASK-177 — Use one coherent public Aitta frame on every human public page.** Centralize the product-specific public header, navigation, footer, and page-frame composition so configured and unconfigured home, published permalink, Privacy, Technical, storage-unavailable, and global/draft/unknown not-found states reuse the same maintained chrome. DoD: keep one stable public-header navigation across public routes instead of replacing `Manage` with page-specific actions; keep Privacy, Technical, and GitHub available in the common footer and only the powered-by attribution owner-hideable; preserve every route, metadata, API, D1, owner destination, draft-unknown parity, CSP and cache behavior; leave the distinct authorized owner workspace and safe access states to TASK-182; add no generic UI framework, theme system, client router, sticky obstruction, or fake destination; prove every named public page/state at 320/390/1440 pixels and 400-percent reflow with native keyboard/focus, 44-pixel touch targets, forced colors, reduced motion, no overflow/private canary/console error, focused shared-chrome regression tests and an acceptance record, migration-drift/full-validation/audit/diff gates, and no external mutation. Depends on: TASK-176.
- [ ] **TASK-178 — Negotiate a browser-readable discovery manifest.** Add one small product-specific `Accept` negotiation contract and make `/.well-known/aitta-social.json` return a no-store `303 See Other` to a shared-frame `/technical/manifest` view only when HTML wins, while preserving raw JSON at the machine URL for missing `Accept`, wildcard, explicit JSON, and `?format=json` requests. DoD: select only by parsed media ranges, quality, specificity, exclusions, and a JSON-on-tie server preference—never User-Agent; let `?format=json` explicitly override negotiation, return 406 when neither supported representation is acceptable, and set `Vary: Accept` on the 303, JSON success/error, and 406 responses; keep the destination HTML no-store with the fixed CSP and link it back to exact raw JSON; retain the exact JSON allowlist, status/error semantics, public cache duration, challenge boundary, canonical source, and protocol 1.0 meaning; prove configured, unconfigured, invalid-canonical, storage-failure, challenge, hostile/malformed/q-zero/tied Accept, browser redirect/navigation, CSP, private-canary, 320-pixel and 400-percent-reflow cases; document the representation decision and acceptance evidence; pass migration-drift/full-validation/audit/diff gates; and make no schema, owner, Site, data, setting, access, domain, Hub, or sibling change. Depends on: TASK-177.
- [ ] **TASK-179 — Negotiate a browser-readable public profile resource.** Apply the accepted negotiation contract to `/api/v1/site` so an explicit HTML winner receives an empty, origin-relative, no-store `303 See Other` to `/technical/profile` before any D1 read, while default and JSON-preferring clients retain the exact protocol 1.0 resource. DoD: make the noindex/no-store shared-frame destination—not the API redirect—render configured data only from `publicSiteResource`, plus fixed no-profile, invalid-canonical, and caught storage-unavailable human states without exception text, success links, or private values, and link configured data to exactly `/api/v1/site?format=json`; preserve the exact JSON allowlist, envelope, errors, canonical URL, account-type compatibility field, presentation values, status and cache duration; inherit JSON-on-tie, `?format=json`, 406, and `Vary: Accept` behavior from TASK-178, ensure an uncaught or narrowly caught JSON-path storage failure is still no-store and carries `Vary: Accept` without inventing a new JSON error envelope, never copy arbitrary query input, use User-Agent, or derive a Location/canonical from request host, and never expose owner/runtime/private fields; prove exact 303 location/no-body, configured/unconfigured/invalid-canonical/storage-failure, every supported Accept decision, CSP, navigation, private canaries, 320-pixel and 400-percent reflow; update protocol/tests/acceptance evidence; pass migration-drift/full-validation/audit/diff gates; and make no schema, owner, Site, data, setting, access, domain, Hub, or sibling change. Depends on: TASK-178.
- [ ] **TASK-180 — Negotiate a browser-readable published-updates collection.** Apply the accepted negotiation contract to `/api/v1/entries` so an explicit HTML winner receives a no-store `303 See Other` to `/technical/updates` with only normalized `page` and `pageSize`, while default and JSON-preferring clients retain the exact protocol 1.0 collection. DoD: validate pagination before redirect so invalid values retain the current JSON error; preserve deterministic order, page and page-size bounds, exact JSON allowlists/envelopes/links/errors/cache, configured canonical source, and complete draft exclusion; set `Vary: Accept` on every negotiated result, provide human previous/next navigation and an exact raw path using `format=json` without copying other query input or request origin, never use User-Agent, and expose no total, private count, identifier, setting, or canary; prove empty/one/many/next/previous/invalid pagination, unconfigured/invalid-canonical/storage-failure, all supported Accept decisions, exact 303 location/no-body, CSP, navigation, 320-pixel and 400-percent reflow; update protocol/tests/acceptance evidence; pass migration-drift/full-validation/audit/diff gates; and make no schema, owner, Site, data, setting, access, domain, Hub, or sibling change. Depends on: TASK-178.
- [ ] **TASK-181 — Negotiate browser-readable published-update detail.** Apply the accepted negotiation contract to `/api/v1/entries/{id}` so an explicit HTML winner receives a no-store `303 See Other` to the origin-relative existing `/entries/{encoded-id}` permalink before any D1 existence read, while default and JSON-preferring clients retain the exact protocol 1.0 detail resource. DoD: preserve the exact JSON allowlist/envelope/error/cache and canonical URL, all four update kinds, destination safety, and published-only draft/unknown indistinguishability; set `Vary: Accept` on every negotiated result, make draft/unknown/deleted HTML requests receive the same redirect and subsequent generic not-found page, provide an exact raw path using `format=json`, never use User-Agent or request-host identity, and never leak an existence signal, draft identifier, owner/runtime value, or private canary; prove published note/article/link/announcement, draft/unknown/deleted, missing profile, invalid canonical, storage failure, all supported Accept decisions, exact 303 location/no-body, CSP, native navigation, 320-pixel and 400-percent reflow; update protocol/tests/acceptance evidence; pass migration-drift/full-validation/audit/diff gates; and make no schema, owner, Site, data, setting, access, domain, Hub, or sibling change. Depends on: TASK-178.
- [ ] **TASK-182 — Reuse the common Aitta chrome in the private owner workspace.** Adapt the authorized `OwnerShell` and safe owner-access states to the same maintained presentational header, footer, link, focus, touch, and responsive primitives established by the public frame while retaining an unmistakably private administration context and owner-specific navigation. DoD: keep exactly one main landmark and one logical skip target; retain the distinct three-link Home, Identity, and New update navigation with active-page semantics, plus View Aitta and the exact existing sign-out destination; make Privacy, Technical, and GitHub at least 44 pixels and reachable through the common footer without making shared primitives read auth, D1, profile, or runtime state; preserve authorization-before-D1 behavior and expose no owner identity, draft, protected configuration, or authorization-result canary in safe access states; preserve every owner route, mutation, API, D1, public projection, CSP, metadata, and cache boundary; add no generic UI framework, client router, public/owner authorization coupling, fake destination, duplicate navigation landmark, or sticky obstruction; prove configured owner home/profile/new/edit, signed-out redirect, non-owner, and missing-owner states at 320/390/1440 pixels and 400-percent reflow with native keyboard/focus, 44-pixel touch targets, forced colors, reduced motion, no overflow/private-canary/console error, focused shared-chrome and owner-security regressions and an acceptance record, migration-drift/full-validation/audit/diff gates, and no external mutation. Depends on: TASK-170, TASK-177.
- [ ] **TASK-183 — Restore reviewed engineering-policy publication.** Use an ordinary authenticated, non-rewriting push to make reviewed local multi-agent-policy commit `ec96d4d` and the completed TASK-184 guidance commit reachable from `origin/develop`. DoD: verify the remote branch contains both commits and their local/remote policy bytes match; if authentication remains unavailable after TASK-184, retain this task as the explicit external blocker rather than treating the recorded failure as completion; and make no `main`, application, schema, migration, Site, deployment, data, setting, access, DNS, domain, Hub, sibling, or runtime change. Depends on: TASK-184.
- [ ] **TASK-185 — Make compiled-Worker response-lifetime tests deterministic.** Repair only the existing Miniflare acceptance tests so each dispatched response body is consumed, parsed, or cancelled before another request can invalidate it. DoD: retain every existing assertion and product behavior while limiting changes to the affected upgrade, presentation-accent, and public functional-matrix tests plus a focused acceptance record; prove those tests under `MINIFLARE_ASSERT_BODIES_CONSUMED=true`, repeat the upgrade proof, and pass migration-drift, full validation, production audit, and diff gates. Make no application, API, protocol, schema, migration, Worker/runtime, Site, deployment, data, setting, access, DNS, domain, Hub, or sibling change. Depends on: none.
- [ ] **TASK-186 — Refine the next parallel-safe server-source boundary improvement.** Read-only inspect the current `app/api`, `lib`, and `db` production-module boundaries outside active task ownership to identify whether one smaller source refactor would materially improve responsibility, testability, or independent feature ownership. DoD: map the relevant module/import/test seams; record either a concrete no-refactor conclusion or exact proposed bounded follow-on rows with direct dependencies, exclusive file ownership, binary behavior-preserving DoDs, and required test/documentation evidence; reject generic frameworks, speculative extension points, and refactors that overlap an active task; add a focused refinement record, pass instruction/plan/diff checks, and make no production-code, protocol, schema, migration, Worker/runtime, Site, deployment, data, setting, access, DNS, domain, Hub, or sibling change. Depends on: none.
- [ ] **TASK-187 — Refine safe owner-managed website replacement.** Define the smallest Aitta-specific runtime customization contract that lets the sole owner reproduce an existing public website's content and visual identity without a repository fork: custom public pages and homepage selection, collision-free paths, configurable header/navigation/footer, bounded styling, and same-origin assets, while preserving fixed owner/security/privacy/protocol boundaries. DoD: inspect current public/owner frames, routes, D1/profile/entry models, mutation guards, fixed CSP/metadata/cache policy, and the supplied public site only as a read-only design reference; explicitly decide versioned persisted page/shell/design representations, draft/publish lifecycle, homepage selection, reserved system/auth/API/static routes and configured-canonical URL construction, safe HTML import/rendering boundary, CSS isolation/property/resource rules, asset storage/upload/serving ownership, import limits, and any required hosting approval, mandatory Manage/Privacy/Technical/GitHub discoverability, accessibility limits, size/count/depth bounds, migration/rollback/protocol decisions, and threat model; write dependency-ordered focused follow-on rows with exclusive file/schema/external-state ownership and binary DoDs, or record an exact external blocker; update only superseded customization policy guidance if the bounded model requires it; add a focused refinement/acceptance record; pass instruction/plan/diff gates; and make no product code, schema/migration, Site, deployment, data, setting, access, DNS, domain, Hub, or sibling change. Reject arbitrary JavaScript, remote fetch, generic plugins/templates, and global unsafe style injection. Depends on: none.
