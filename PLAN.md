# AittaSocial presence plan

This is the accepted, unfinished queue. IDs are stable. Finished work moves to
`CHANGELOG.md`; unscheduled possibilities belong in `BACKLOG.md`. Every item is
a bounded vertical slice whose definition of done includes its relevant
implementation, negative tests, security and privacy review, accessibility,
migration and protocol decisions, documentation, and decisive evidence.

## Audit basis

- On 2026-08-09 the repository POC and public Site were rechecked. Public
  profile and published-update HTML, protocol 1.0 discovery and `/api/v1`, the
  sole-owner boundary, draft privacy, deployment-owned D1, native navigation,
  responsive behavior, and Hub-failure isolation are working and remain in
  scope without being rewritten.
- The deployed Site version 6 uses product source commit
  `3d028321f26cda7ce1e4cdbffc64406bb52e4ff5`; at audit time the branch head
  `4ac8cde` added only the completed checkpoint record. The live page still
  uses generic account terminology and generic software metadata, confirming
  the first presence-focused gaps below.
- No accepted presence-consumable Hub contract currently exists. The Hub's
  current documented API is its legacy centralized model and explicitly
  defers independent-presence registration and network credentials. Contract-
  gated tasks below must consume exact accepted Hub schemas and mirrored
  fixtures; they must not guess endpoints, claims, tokens, or data ownership.
- No external Hub task identifier or contract version is cited while no
  presence-consumable contract is accepted. Before a contract-conformance task
  can complete, record the exact Hub source commit, completed Hub task IDs,
  contract paths and versions, and mirrored-fixture digest. Without that
  provenance the task remains blocked; placeholder IDs and guessed artifacts
  are prohibited.
- The planning checkout passes `npm run validate` with the instruction,
  license, plan, instance, runtime, D1 migration, type, lint, production-build,
  and 81 focused test gates green; `npm audit --omit=dev` reports no production
  dependency advisory.
- `ROADMAP-001` through `ROADMAP-003` and `BACKLOG-001` through `BACKLOG-003`
  were promoted into this task graph. Their stable mappings remain provenance
  as individual tasks move from this queue to `CHANGELOG.md`; the source files
  do not duplicate them as unscheduled work or current capability claims.

## Active queue

- [ ] **TASK-035 — Adopt presence-oriented language and a truthful local-owner entry.** Presence-only work promoted from ROADMAP-001: replace human-facing account, entries, deployment overview, and account profile wording with presence, updates, Your presence, and Identity where accurate, replace generic public Sign in copy with an owner-management entry that cannot imply Hub identity or network membership, and keep the existing challenge/root probe visibly labeled as provisional Hub setup rather than a completed Hub connection until TASK-043 replaces it; preserve internal models, route names, protocol 1.0, dispatcher-owned authentication, and every server-side owner check. DoD: ship the coherent public and owner copy/navigation slice with positive and negative rendering and authorization tests, native-navigation proof, security/privacy/accessibility review at wide and 320-pixel widths, synchronized AGENTS and product/setup documentation, an explicit no-schema/no-migration/no-public-protocol-change decision, and passing repository validation. Depends on: none.
- [ ] **TASK-036 — Make ordinary identity setup category-neutral without breaking protocol 1.0.** Presence-only work promoted from ROADMAP-001: remove the required owner-category choice and category label from ordinary setup and public identity presentation while preserving existing stored profiles and deciding the smallest truthful compatibility treatment for the published `accountType` field and manifest. DoD: implement and document the selected neutral behavior for new and legacy profiles, retain exact public allowlists, add focused category-neutral HTML/manifest/`/api/v1/site` compatibility tests while keeping the existing validation/privacy/canonical regression suite green, prove owner/non-owner/missing-owner form behavior and accessible labels, generate and review a non-destructive migration only if the chosen schema requires one, record the additive-compatibility or new-version decision in the protocol, and pass full validation. Depends on: TASK-035.
- [ ] **TASK-037 — Make metadata and checked-in identity assets represent the presence.** Presence-only work promoted from ROADMAP-001: derive the document title, description, canonical metadata, and sharing semantics from the configured public profile and canonical URL rather than an untrusted request host or generic software identity, and use a typographic default plus only direct checked-in source assets for any favicon, logo, or social preview; add no runtime asset resolver, configuration abstraction, upload UI, R2, media manager, or new storage subsystem. DoD: implement populated and safe unconfigured metadata, hostile-host and private-canary tests, canonical/cache/security review, accessible alternative text, build and packaged-asset checks, plain-language source-customization documentation, an explicit no-D1-migration/public-protocol-change decision, and passing full validation. Depends on: TASK-035.
- [ ] **TASK-038 — Refine the strong default around identity, featured information, and recent updates.** Presence-only work promoted from ROADMAP-001: reuse the existing profile and published-entry model to make those three areas the balanced public hierarchy, preserve the already-proven layout, styling, intentional empty state, and restrained owner-hideable attribution unless a focused hierarchy test requires a change, and move manifest and JSON links to a secondary technical context without adding a theme engine, content subsystem, or network placeholder. DoD: implement only the necessary hierarchy and technical-link refinement, test empty/populated and all update kinds with draft/private canaries, verify long names/descriptions/URLs, semantic order, contrast, focus, touch targets, native links, 320-pixel layout, 400-percent zoom, reduced motion, and Hub-unavailable behavior, update presentation and setup documentation, record no schema/protocol migration, and pass full validation. Depends on: TASK-035, TASK-036.
- [ ] **TASK-039 — Deliver a guided first-owner journey and complete reusable creation prompt.** Presence-only work promoted from ROADMAP-001: guide the authorized sole owner through identity, featured information, an initial update, and public preview using existing durable state; keep runtime/protocol/Hub material in advanced setup, invite later plain-language ChatGPT customization, and never derive public identity from ChatGPT identity. DoD: implement useful fresh, partial, and complete journey states plus the concise README prompt covering unique-Site reuse or creation, multiple-match stop, private-first setup, own D1/no external database, protected owner-email handling, Hub independence, the identity-neutral strong default, customization invitation, and separate public/domain approvals; test signed-out, owner, non-owner, missing-owner, draft privacy, resume behavior, responsive/keyboard/touch/zoom states, security/privacy/accessibility, documentation and no-migration/protocol decisions, and pass full validation. Depends on: TASK-036, TASK-037, TASK-038.
- [ ] **TASK-040 — Verify the deployable presence-first milestone.** Presence-only acceptance for ROADMAP-001: verify TASK-035 through TASK-039 as one source-provenance-bound release candidate without changing the Hub or inventing network capability. DoD: run a clean install, production dependency audit, migration generation and hash review, full validation, and Sites packaging; prove a fresh private template path from the bare repository URL and a production-equivalent in-place upgrade from the current POC fixture, preserving owner authorization, profile, drafts, published updates, canonical links, and D1 data; document software/protocol support boundaries, upgrade/rollback behavior, and hosted-backup limitations; prove empty, non-owner, missing-configuration, and Hub-unavailable states with production-equivalent fixtures or an explicitly approved disposable checkpoint; with explicit owner approval only, save and deploy the exact reviewed commit to the existing Site without changing its content, protected settings, access, DNS, or custom domains, then inspect its unchanged configured/populated public and owner states; cover wide, 320-pixel, keyboard, touch, 400-percent zoom, reduced-motion, privacy, console, metadata, manifest, API, and native navigation, record status and provenance, and add each discovered correction as its own unchecked task before fixing it. Depends on: TASK-039.
- [ ] **TASK-041 — Accept and pin the Hub consumer profile for public presence discovery and capabilities.** Contract-conformance work promoted from ROADMAP-002; external prerequisite: Hub publishes an accepted versioned consumer/conformance profile, examples, stable errors, and malicious/outdated/offline fixtures for the presence-owned public discovery protocol. DoD: record the completed Hub task IDs, source commit, contract paths and versions, and mirrored-fixture digest; review and mirror the exact accepted artifacts without a shared runtime package, pin their version and canonical `https://aitta.social` authority, prove current protocol 1.0 compatibility and explicit allowlists with conforming, modified, outdated, partial, oversized, unavailable, and malicious fixtures, document origin/capability/cache/privacy/accessibility behavior, add no production route, UI, outbound call, schema, migration, or live control in this contract-only slice, and pass validation. Depends on: none.
- [ ] **TASK-042 — Accept and pin the Hub registration, proof, credential, and lifecycle contracts.** Contract-conformance work promoted from ROADMAP-002; external prerequisite: Hub publishes accepted versioned schemas and vectors for signed exact-origin proof, state, PKCE, one-time code exchange, scoped registered-presence/deployment credentials, issuer/audience binding, expiry, rotation, revocation, disconnection, reconnect, transfer, and recovery. DoD: record the completed Hub task IDs, source commit, contract paths and versions, and mirrored-fixture digest; mirror and test the exact artifacts, splitting initial-connection and later lifecycle acceptance into separate tasks first if Hub does not publish them as one coherent versioned contract; require that Hub return only the minimum opaque or pseudonymous subject needed for an authorized network action and never raw Hub email or name; decide server-only key and credential generation/storage/encryption/deletion in deployment-owned D1 unless a verified Sites secret-write boundary exists; distinguish generated D1 migration and rollback for new local server-only state from owner-executed transition and rollback instructions for `AITTA_SOCIAL_HUB_CHALLENGE`, legacy protected deployment-credential input, the optional manifest challenge, `/api/private/hub/test`, the owner probe UI, and related documentation, while application code neither mutates nor claims to delete protected runtime settings; cover replay/nonce/code reuse, state/PKCE/CSRF/session-fixation/confused-deputy/origin/callback/issuer/audience/scope/expiry/leakage failures, add no production route, UI, outbound call, migration, or live control in this contract-only slice, and pass validation. Depends on: TASK-041.
- [ ] **TASK-043 — Deliver the owner-authorized secure Hub connection.** Presence implementation promoted from ROADMAP-002; external prerequisite: a review Hub service with recorded deployed source revision, contract version, and review endpoint implements the initial-connection artifacts pinned by TASK-042. DoD: make presence-started and Hub-started owner flows converge on one independently authorized same-origin presence endpoint, generate and confine deployment keys and protocol state server-side, publish or answer at the exact canonical origin with the accepted current public key and single-use challenge so Hub proves origin control rather than trusting a claimed URL, complete signed proof and state/PKCE/one-time server exchange without pasted secrets, expose only the accepted minimal opaque or pseudonymous Hub subject and never raw Hub email or name, and show a safe registration receipt and immediate revoke/disconnect control; prove registration creates no follow or reciprocal edge, indirect link, or suggestion permission; only after the accepted connection passes contract-backed end-to-end validation, remove legacy `AITTA_SOCIAL_HUB_CHALLENGE` consumption and publication, legacy protected deployment-credential ingestion, `/api/private/hub/test`, and the root-probe UI, tests, and documentation; preserve the discovery manifest and any Hub-origin setting required by the accepted contract, make the public protocol/versioning decision explicit, and require separate owner approval for removing hosted protected settings; keep local management and public reads independent of Hub, test signed-out/non-owner/IDOR/CSRF/replay/malicious-Hub/partial-failure/key-and-credential canaries plus accessible responsive success/denial/error states, review migration/rollback, and pass local and contract-backed end-to-end validation before exposing the control. Depends on: TASK-042.
- [ ] **TASK-044 — Complete Hub connection rotation, reconnect, transfer, and recovery behavior.** Presence implementation promoted from ROADMAP-002; external prerequisite: a review Hub service with recorded deployed source revision, contract version, and review endpoint implements the lifecycle artifacts pinned by TASK-042, including key loss, canonical-origin change, ownership collision, and atomic Hub-side revocation of previous credentials and sessions. DoD: require current local-owner authorization and Hub reauthentication where specified, preserve exact-origin/key continuity or mandatory reverification, invoke the exact idempotent Hub lifecycle operation, activate replacement local state only after a verified receipt, and fail closed with recoverable idempotent local state on uncertain or partial cross-service results; test cross-owner IDOR, replay, stale keys, concurrent actions, disconnection, local-owner-email change, Hub-identity deletion, presence decommissioning, canonical-origin change, transfer, key or credential loss, recovery, and leakage without implying Hub can modify or delete the independent presence; provide accessible confirmations and receipts, generate/review only necessary non-destructive migrations, document privacy/retention/operator limits, and pass contract and full validation. Depends on: TASK-043.
- [ ] **TASK-045 — Accept and pin the verified-presence directory and resolution contract.** Contract-conformance work promoted from ROADMAP-002, ROADMAP-003, and BACKLOG-003; external prerequisite: Hub publishes an accepted versioned catalog/resolution contract for opted-in verified canonical origins, minimal display metadata, capabilities, visibility/enforcement state, pagination, and stable failures. DoD: record the completed Hub task IDs, source commit, contract paths and versions, and mirrored-fixture digest; mirror exact schemas and fixtures, prove no local speculative index and no identity/content-truth claim, validate canonical HTTPS origins and safe escaped metadata, cover hidden/disabled/removed/stale/offline/incompatible/malicious records and private canaries, document cache/retention/privacy/accessibility and Hub-failure behavior, decide no local migration until real persisted state is required, add no production route, UI, outbound call, schema, migration, or live control in this contract-only slice, and pass validation. Depends on: TASK-041.
- [ ] **TASK-046 — Let an owner discover verified presences.** Presence implementation promoted from ROADMAP-003 and BACKLOG-003; external prerequisite: the public Hub directory with recorded source and endpoint provenance implements the exact TASK-045 contract at its fixed authority. DoD: add a focused sole-owner-authorized search/browse experience using only bounded, escaped, verified directory metadata and exact canonical links, show empty/stale/hidden/offline/incompatible states without a local index or popularity ranking, keep suggestions and follows absent until their own tasks, enforce server-side destination/origin bounds and private no-store behavior, test signed-out/non-owner/IDOR denial, malicious metadata, pagination, Hub outages, identity/credential canaries, keyboard/touch/focus/320-pixel/zoom/reduced-motion states, update security/privacy/user docs and migration decision, and pass contract, integration, and full validation. Depends on: TASK-045.
- [ ] **TASK-047 — Accept and pin acting-presence session, follow, visibility, and reader-boundary contracts.** Contract-conformance work promoted from ROADMAP-002, ROADMAP-003, and BACKLOG-001; external prerequisite: Hub publishes accepted versioned contracts and fixtures for Hub-issued network sessions, explicit acting-presence authorization, follow/unfollow, private-by-default relationship visibility, direct/indirect link queries, rate limits, and the interaction bounds that consume TASK-045 verified-origin/capability resolution for presence-owned public update retrieval. DoD: record the completed Hub task IDs, source commit, contract paths and versions, and mirrored-fixture digest; mirror exact schemas and vectors, require exact issuer/audience/subject/presence/scope/expiry/revocation and strict CORS/origin behavior without exposing raw Hub email or name, prove Hub graph authority and one-way explicit edges, encode the exact `A→B→C` and `B→A` plus `B→C` indirect-candidate vectors with self and already-followed exclusions, no stored indirect edge, recursive traversal, automatic/reciprocal link, or popularity count, and intermediary disclosure only when the relationship owner permits it, preserve privacy-safe indirect semantics, stable errors, and failure isolation while leaving entry pagination/cursor/tombstone/content semantics to TASK-049, test IDOR/replay/session-fixation/visibility inference/abuse/outdated/malicious fixtures, document retention/privacy/accessibility/migration responsibilities, add no production route, UI, outbound call, schema, migration, or live control in this contract-only slice, and pass validation. Depends on: TASK-042, TASK-045.
- [ ] **TASK-048 — Deliver explicit follow, unfollow, and relationship privacy controls.** Presence implementation promoted from ROADMAP-003 and BACKLOG-001; external prerequisite: a review Hub service with recorded deployed source revision, contract version, and review endpoint implements the exact TASK-047 follow/session contract. DoD: provide an observable Follow entry from a Hub-verified target, treating any submitted canonical URL only as an untrusted lookup key that TASK-045 resolution must verify, then initiate and mutate relationships only through the exact accepted Hub authorization flow for an explicitly selected acting presence; never treat target-presence local ChatGPT sign-in as network authentication, create only deliberate one-way edges, support unfollow and owner-controlled visibility, keep Hub authoritative while this deployment stores or mutates no follow edge and shows no popularity counts, enforce server-side authorization for every presence-side action plus the accepted session/origin/method/body/rate-limit/IDOR boundaries, test signed-out/non-owner/IDOR denial, duplicates/concurrency/revocation/private-edge inference/outages/malicious targets and accessible confirmation/error states, remove only the now-real follow/relationship exclusions from AGENTS and documentation, document privacy/retention/migration, and pass end-to-end and full validation. Depends on: TASK-043, TASK-047.
- [ ] **TASK-049 — Conform public update retrieval to the accepted reader contract.** Presence-owned protocol work promoted from ROADMAP-003 and BACKLOG-002: consume the locally pinned network-reader boundary from TASK-047 while retaining this repository's ownership of public discovery and update-retrieval semantics. DoD: publish a documented conformance decision proving public protocol 1.0 page pagination sufficient or implement only the exact accepted backward-compatible cursor, conditional-read, tombstone, or new-version capability with explicit negotiation and stable provenance/errors, retain existing `/api/v1` behavior and draft-indistinguishable privacy, use deterministic prepared D1 queries and a reviewed migration/index only when justified, test ties, edits, unpublishing/deletion, clock skew, stale/outdated clients, canonical URLs, limits and private canaries, update protocol/security/privacy docs, and pass conformance and full validation. Depends on: TASK-047.
- [ ] **TASK-050 — Deliver the private Your network reader.** Presence implementation promoted from ROADMAP-003 and BACKLOG-002; external prerequisite: Hub review services with recorded deployed source revision, contract version, and review endpoint implement the TASK-045 and TASK-047 resolution, session, and follow contracts. DoD: require sole-owner server authorization and private no-store projections, accept no browser-supplied remote destination, send Hub/network credentials only to the exact accepted Hub origin and never to remote presences or redirects, resolve only Hub-verified origins, retrieve remote updates anonymously through the accepted public presence protocol, and never send Hub, network, or local credentials to a remote presence or redirect; fetch bounded authoritative public updates without a durable shared content copy, and show deterministic newest-first updates with stable tie-breaking, deduplication, provenance, stale/partial-failure states, and suggestions kept separate; enforce HTTPS/exact-origin and SSRF, private/link-local/reserved/rebinding, redirect, content-type, response-size, timeout, fan-out, concurrency, schema, escaping, and external-link defenses; test signed-out/non-owner/IDOR denial, no-follows/loading/offline/slow/malicious/incompatible/deleted content, session revocation, canary leakage and Hub outage isolation plus keyboard/touch/320-pixel/zoom/reduced-motion behavior; narrow the durable combined-reader exclusion only when this slice is real, document cache/retention/privacy/migration, and pass end-to-end and full validation. Depends on: TASK-048, TASK-049.
- [ ] **TASK-051 — Accept and pin the privacy-preserving suggestions contract.** Contract-conformance work promoted from ROADMAP-003; external prerequisite: Hub publishes an accepted versioned suggestions schema and fixtures based only on permitted direct/indirect relationship data, with displayable reasons, opt-out, pagination, abuse limits, and stable errors. DoD: record the completed Hub task IDs, source commit, contract paths and versions, and mirrored-fixture digest; mirror and test exact artifacts, prove suggestions never create or imply a follow, never expose popularity counts, never recurse beyond allowed paths, and never reveal a private edge or named intermediary without permission, cover sparse/hidden/disabled/removed/stale/malicious/deterministic cases and private canaries, document privacy/retention/ranking inputs/accessibility and no local persistence decision, add no production route, UI, outbound call, schema, migration, or live control in this contract-only slice, and pass validation. Depends on: TASK-047.
- [ ] **TASK-052 — Show separate privacy-safe presence suggestions.** Presence implementation promoted from ROADMAP-003; external prerequisite: a review Hub service with recorded deployed source revision, contract version, and review endpoint implements the exact TASK-051 suggestions contract. DoD: render a bounded deterministic owner-only suggestions section separate from followed updates, show only permitted named or non-identifying reasons, provide clear opt-out/stale/empty/error states, require a separate explicit TASK-048 Follow action for every new edge, treat all metadata as untrusted, preserve local/public operation through Hub failure, test signed-out/non-owner/IDOR denial, private-edge inference, malicious content, pagination, rate limits, canary leakage, keyboard/touch/focus/320-pixel/zoom/reduced-motion behavior, narrow the durable recommendation exclusion only for this accepted privacy-preserving presence-suggestion capability, update privacy/security/user docs and migration decision, and pass integration and full validation. Depends on: TASK-048, TASK-051.
- [ ] **TASK-053 — Verify the connected presence phase on disposable checkpoints.** Coordinated acceptance promoted from ROADMAP-002 and ROADMAP-003; external prerequisites: compatible Hub and remote-presence review endpoints implement every pinned contract, and the owner explicitly approves each checkpoint deployment and test-data action. DoD: use only disposable non-production identities, presences, relationships, and updates to prove both Hub-connection entry points, exact-origin proof, no pasted secrets, acting-presence isolation, registration without automatic follows, discovery, follow/unfollow/privacy, deterministic Your network retrieval, separate suggestions, rotation/revocation/disconnect/transfer/recovery, malicious/offline/outdated counterpart handling, Hub outage independence, migrations/rollback and secret/private-canary scans; inspect signed-out/owner/non-owner, wide/320-pixel, keyboard, touch, 400-percent zoom, reduced-motion, console, logs, APIs and storage; record exact source/deployment provenance; add every correction as a new task before fixing it; and make no production, access, DNS, custom-domain, protected-setting, or production-data change without separate approval. Depends on: TASK-040, TASK-044, TASK-046, TASK-050, TASK-052.
